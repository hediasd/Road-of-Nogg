# Implementation plan

No cycle is open.

**Cleared 2026-08-25.** The previous contents were the Battle Legibility Cycle,
opened 2026-08-15 against a legibility review of `Battle25D`. Its ten items
shipped hover-driven status readout and reach painting in every phase, distinct
status-effect silhouettes with resting colour chips, the turn order as a top
portrait rail that crosses the round boundary and previews an uncommitted speed
change, per-enemy attribution of the danger zone, and a held-key deep card
carrying spells, passives, JUMP, LUCK, the race elemental matchup, and
hits-to-kill. Three items were withdrawn by user decision — the board-space unit
plate, its design contract, and the pending-damage preview that depended on it.
Consolidated validation passed the same day this file was cleared; the full item
text and Resolution notes are recoverable with
`git show 99ceec7:implementation_plan.md`.

Genuinely open work was moved to `BACKLOG_CRITICAL.md` and named to the user:
health is still readable for at most two units out of eight, and a spell blocked
by anything other than a cooldown still reads `CD 0`.
