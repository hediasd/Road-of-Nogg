extends Object

class_name Reference

#static var list

#static func getReference(name) -> Variant:
#	for entry in list:
#		if(entry["NAME"] == name):
#			print("comparing %s and %s", entry["NAME"], name)
#			return entry
#	return list.back()
